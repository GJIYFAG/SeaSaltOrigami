# SeaSaltOrigami 项目补充说明

## 1. 写在前面

这份补充说明不是为了替代原有的项目说明，而是作为一份更偏“展开讲讲”的辅助文档。原有说明已经能把项目的基本信息交代清楚了，但在实际阅读过程中，很多人往往还会想知道：这个项目为什么会这么分层、界面和规则是怎么配合的、为什么要单独保留控制器层、以及后续扩展时应该优先看哪里。

所以这份文档主要做一件事：把一些原本一句话能说完的内容，稍微多说几句。它不追求特别正式的技术规范风格，更像是一个方便快速了解项目状态的补充阅读材料。

## 2. 项目的整体思路

SeaSaltOrigami 的结构并不复杂，但它的分工比较清晰。游戏本身属于典型的“规则驱动型”项目，核心价值不在于某一个单独的 UI 页面，而在于一整套能够稳定运行的规则、状态与反馈链路。换句话说，玩家看到的界面只是结果，真正决定游戏体验的，是底层规则是否可靠、状态切换是否自然、回合推进是否顺畅。

因此项目在设计上把逻辑层和表现层分开：

- C++ 负责规则、数据和流程控制。
- QML 负责展示、交互和动画。
- 控制器层负责把规则状态转换成界面可以直接消费的数据。

这种方式的好处在于，很多改动不会互相牵连。比如规则调整时，通常只需要关注 `GameRule`、`Player` 和 `CardPile`；而界面排版变化，则更多会落在 `qml/` 目录中。这样拆开之后，整体维护成本会低不少。

## 3. 为什么会这样分文件

如果只看文件数量，项目看起来像是“卡牌游戏常见结构”，但实际上每个模块的职责边界还是比较明确的。

### 3.1 卡牌与牌堆

`Card` 负责描述“这张牌是什么”。它不仅包含名称、主类型、子类型和颜色，也包含一些与规则相关的基础属性。它的作用不是做决策，而是提供足够明确的数据，让规则层能够根据牌的信息判断后续行为。

`CardPile` 则负责“牌从哪里来、去了哪里”。这一层是整个游戏运行节奏的底座，因为无论是主牌库抽牌、弃牌堆回收，还是特殊效果导致的牌堆变化，最终都要落到这里处理。只要牌堆行为稳定，很多上层逻辑就会自然稳定。

### 3.2 玩家状态

`Player` 主要负责保存某个玩家在一局游戏中的状态。这里包括手牌、收集牌、增益牌、锁定牌、对牌区，以及各种分数和回合标记。它的任务可以理解成“把一个玩家当前的所有状态装在一起”，方便规则层在进行回合结算时直接读取。

这种设计的优点是状态集中，不会散落到很多地方；缺点则是字段会比较多，所以当规则继续增加时，维护时要特别注意每一个状态位的重置和切换。

### 3.3 规则层

`GameRule` 是最核心的一层。它的职责不是简单判断输赢，而是承载整局游戏的流程：发牌、出牌、事件触发、回合结算、轮次切换、胜负判断等，都属于这里的工作范围。

从结构上看，`GameRule` 更像“规则引擎”，而不是“界面辅助工具”。这意味着它不应该直接关心界面怎么展示，只需要关心规则是否按预期运作。这样的分离可以避免 UI 变动反向污染规则实现。

### 3.4 控制器层

`SeaSaltGameController` 是项目里很关键的桥接层。QML 不会直接操作底层规则对象，而是通过这个控制器读取状态、触发动作、接收信号。这样做的好处是，界面层不需要了解太多规则细节，只要知道“当前显示什么”“按钮是否可用”“某个动作是否可以执行”就可以了。

在实际开发中，控制器层往往承担了不少看不见但很重要的工作，比如：

- 把内部状态转换成适合界面绑定的字符串或列表。
- 对操作进行前置判断，避免界面误触发非法流程。
- 统一发送动画和提示信号，让交互表现更一致。

## 4. 界面层的作用

QML 部分并不是单纯的“把按钮摆出来”，它更像是整套交互节奏的外壳。卡牌游戏的体验，很大程度上来自反馈速度和信息清晰度：玩家要能快速看懂当前轮到谁、自己有哪些牌能用、这次操作会影响哪里、以及当前回合离结束还有多远。

因此界面文件虽然看起来只是若干组件拼起来，但它们实际上承担了非常具体的职责：

- 主界面负责组织整体布局。
- 菜单和设置页负责进入与配置。
- 卡牌组件负责统一视觉表现。
- 手牌、牌堆、已打出对牌和收集区负责展示关键状态。
- 提示和动画负责把规则变化变成可感知的交互反馈。

换句话说，QML 的价值不只是“好看”，而是让规则变化被玩家及时理解。一个规则游戏如果反馈不清楚，哪怕逻辑正确，也会显得很难玩。

## 5. 关于测试

当前仓库里有一个基础的规则冒烟测试，它的意义不是覆盖全部逻辑，而是先把最基础的关键路径固定下来。对于规则类项目来说，这类测试非常重要，因为一旦后续有人调整配对逻辑、牌堆抽取逻辑或者玩家状态管理，很容易不小心把一些原本稳定的行为改坏。

冒烟测试的好处是简单直接：它不追求把所有边界情况一次性写完，只要能证明几个最核心的动作仍然可用，就已经能帮助我们提前发现问题。后续如果项目继续增长，测试最好逐步补到以下几类：

- 对牌合法性测试。
- 牌堆抽取与回收测试。
- 玩家回合状态重置测试。
- 结算分数测试。
- 事件效果测试。

这些测试不一定要一次写完，但它们值得作为后续补强的方向。

## 6. 维护时可以重点看的地方

如果后面要继续改项目，通常建议按下面的顺序看：

1. 先看 `GameRule`，因为它决定规则行为是否正确。
2. 再看 `SeaSaltGameController`，因为它决定界面能否正确拿到状态。
3. 然后看 `qml/`，因为那里决定玩家看到什么、怎么操作。
4. 最后再回到 `Player`、`CardPile` 和 `Card`，检查底层数据结构是否跟上了新规则。

这个顺序并不是绝对的，但对排查问题通常会比较高效。很多时候，游戏卡住、按钮不可用、得分显示异常，问题并不一定在最显眼的页面里，而是出在状态传递链的某一环。
